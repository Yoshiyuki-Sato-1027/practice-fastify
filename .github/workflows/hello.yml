name: Hello, World! # ワークフローの名前
on: [push, pull_request] # イベント名 プッシュされるときにワークフローが走る

jobs: # ジョブの定義始まる
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, World!"
